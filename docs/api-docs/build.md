# `biotope build`

Materialise a runnable BioCypher project from `mappings/*.mapping.yaml` and (optionally) `alignment.yaml`. Output is a `build/` directory with `config/schema_config.yaml`, materialised mappings, and a `create_knowledge_graph.py` entry point.

::: biotope.commands.build
