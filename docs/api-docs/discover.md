# `biotope discover`

Rank registered adapters and local Croissant files by overlap with the project's `required_entities`. Backed by `biotope.croissant.registry.client.RegistryClient` — pluggable between local YAML directories and an HTTP endpoint.

::: biotope.commands.discover
