# `biotope benchmark`

Emit quality/coverage metrics for a built graph as JSON. v1 ships the structured skeleton (`coverage`, `id_consistency`, `alignment_yield` slots) so downstream tooling can lock onto the shape; real metric implementations land iteratively.

::: biotope.commands.benchmark
